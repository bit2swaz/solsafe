{
  "name": "solsafe",
  "version": "0.0.0",
  "private": true,
  "description": "SolSafe project scaffold aligned to docs/SSOT.md",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "node --eval \"console.log('Dev entry not configured yet')\"",
    "test": "node --eval \"console.log('No tests configured yet')\"",
    "build": "node --eval \"console.log('Build step not configured yet')\""
  }
}